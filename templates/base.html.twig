<!DOCTYPE html>
<html class="h-100">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Ghost Developers{% endblock %}</title>
        <link rel="icon" type="image/x-icon" href="{{ asset('build/images/favicon.ico') }}" />

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="d-flex flex-column h-100">
        <main class="flex-shrink-0">
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">
                    <img src="{{ asset('build/images/favicon.jpeg') }}" width="30" height="30" alt="logo">
                </a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://github.com/eliasmmata/eliasmmata/blob/main/README.md" target="_blank">
                        <i class="fab fa-github"></i>
                        My Github</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-ghost"></i>
                        About me
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a style="display:flex;justify-content:space-between !important;align-items:center;" class="dropdown-item" href="https://eliasmmataportfolio.netlify.app/" target="_blank">
                        Portfolio
                        <i class="fas fa-briefcase"></i>
                        </a>
                        <a style="display:flex;justify-content:space-between !important;align-items:center;" class="dropdown-item" href="https://www.linkedin.com/in/pabloeliasmorenomata/" target="_blank">
                        Linkedin
                        <i class="fab fa-linkedin-in"></i>
                        </a>
                        <div class="dropdown-divider"></div>
                        <a style="display:flex;justify-content:space-between !important;align-items:center;" class="dropdown-item" href="https://www.instagram.com/eliasmmata/" target="_blank">
                        Instragram
                        <i class="fab fa-instagram"></i>
                        </a>
                        </div>
                    </li>
                    </ul>
                    <form class="form-inline my-2 mr-4 my-lg-0">
                    <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                    </form>
                    {% if app.user %}
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#"
                            role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.alias }}
                            {% if app.user != app.user %}
                            Log In
                            {% endif  %}
                            </a>
                            <ul class="drowpdown-menu dropdown-menu dropdown-menu-end text-center mt-2" style="right: 0; left: auto;">
                            {% if app.user != null %}
                                <a class="dropdown-item" href="{{ path('app_logout') }}"><li>Log Out</li></a>
                            {% endif  %}
                            </ul>
                        </li>
                    </ul>
                    {% endif %}
                </div>
            </nav>
            <h3 class="text-center mt-4">Share your ideas, little pieces of your code,</h3>
            <h3 style="margin-bottom: -1rem !important;" class="text-center">or simply de-stress anonymously!</h3>
            <div class="container gedf-wrapper my-4">
                <div class="row">
                    <div id="secret-detail" class="col-md-12 gedf-main">
                        {% block body %}
                        {% endblock %}
                    </div>
                </div>
            </div>

        </main>
        <footer class="footer mt-auto py-3 bg-light">
            <div class="container text-center">
                <p style="margin:0;padding:0;" class="text-muted"> - Ghost Social Network - </p>
                <span style="font-size:0.75rem; margin:0;padding:0;" class="text-muted"> (Powered by Symfony - Developed By @eliasmmata) </span>
            </div>
        </footer>
    </body>
</html>
